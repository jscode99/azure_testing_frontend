{
  "name": "azure_test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "npm run build && next export -o _static",
    "start": "next start",
    "export":"npm run build && next export -o _static"
  },
  "dependencies": {
    "https": "^1.0.0",
    "next": "11.1.2",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  }
}
